prototype(Carbon.Link:TagName) < prototype(Neos.Fusion:Value) {
	property = 'link'
	linkTagName = 'a'
	defaultTagName = 'span'

	read = ${q(node).property(this.property)}
	value = ${this.read ? this.linkTagName : this.defaultTagName}
}
