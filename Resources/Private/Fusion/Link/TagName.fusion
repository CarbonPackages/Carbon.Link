prototype(Carbon.Link:TagName) < prototype(Neos.Fusion:Value) {
	link = ${link ? link : false}
	linkTagName = 'a'
	defaultTagName = 'span'
	value = ${this.link ? this.linkTagName : this.defaultTagName}
}
